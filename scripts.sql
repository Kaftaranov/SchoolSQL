select name,age from student where age>10 and age <13 group by name, age
select name from student
select * from student where name like '%o%'
select name, id, age from student where age < id
select name,age from student order by age
